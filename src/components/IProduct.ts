export default interface IProduct {
  id: string;
  productId: string;
  productName: string;
  description: string;
  price: number;
  quantity: string;
  imageURL: string;
  videoPath: string;
  createdAt: Date;
}

